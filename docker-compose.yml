version: '2.4'

services:

    postgres_local:
      image: "postgres:13.3-alpine"
      ports:
        - "65432:5432"
      env_file:
        - .env
      volumes:
        - .postgresdata:/var/lib/postgresql/data

